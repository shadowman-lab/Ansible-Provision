---
- name: Update VM on AWS using Terraform
  hosts: localhost
  gather_facts: false

  vars:
    shadowman_provision_hypervisor: "Terraformaws"
    terraform_state: "present"
    terraformmain: "gitops"
    operating_system: "ignore"

  roles:
    - shadowman_provision
